
public class SumaPrimerosNnumeros {
	
	
	//ENTRADA  n
	//SALIDA imprimir desde 0 HASTA N  (0 1 2 3 ... N)
	
	public static void main(String arg[]) {
/*		
  		int n = 4;
		int e = 0;
		System.out.println(e); <- regresar aqui
		e = e +1;
		if(n >= e) {
			//tengo que regresar a la linea del system print
		}else {
			System.exit(0);
		}
		
		PASO 1: CONVIERO EL IF EN FOR
		int n = 4;
		int e = 0;
		System.out.println(e); <- regresar aqui
		e = e +1;
		for(n >= e) {
			//tengo que regresar a la linea del system print
		}else {
			System.exit(0);
		}
		
		PASO 2: tomo el contador y lo pongo al iicio del for
		int n = 4;
		System.out.println(e); <- regresar aqui
		e = e +1;
		for(int e = 0; n >= e) {
			//tengo que regresar a la linea del system print
		}else {
			System.exit(0);
		}
		PASO 3: tomo el que incrementa y lo pongo al final del if
		int n = 4;
		System.out.println(e); <- regresar aqui
		for(int e = 0; n >= e;e = e +1) {
			//tengo que regresar a la linea del system print
		}else {
			System.exit(0);
		}
		PASO 4: todo lo que quedo entre la linea a la que quero regresar y el for lo copio adentro del for
		int n = 4;
		for(int e = 0; n >= e;e = e +1) {
			System.out.println(e);
		}else {
			System.exit(0);
		}
		PASO 5: borro el else y lo que tiene adentro se queda afuera del for
		int n = 4;
		for(int e = 0; n >= e;e = e +1) {
			System.out.println(e);
		}
		System.exit(0);
		
*/
		
		int n = 4;
		for(int e = 0; n >= e; e = e + 1) {
			System.out.println(e);
		}
		System.exit(0);
		
		
	}

}
